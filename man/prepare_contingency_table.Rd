% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca_analysis.R
\name{prepare_contingency_table}
\alias{prepare_contingency_table}
\title{Prepare a contingency table from an edgelist}
\usage{
prepare_contingency_table(edgelist)
}
\arguments{
\item{edgelist}{A tibble with columns \code{group}, \code{from}, and \code{to}.}
}
\value{
A numeric matrix with groups as rows and descriptors as columns.
Zero-sum rows and columns are removed.
}
\description{
Pivots both \code{from} and \code{to} columns of an edgelist into a single descriptor
column and cross-tabulates against \code{group}, producing a matrix suitable for
correspondence analysis. Both endpoints of each edge are treated as concept
descriptors co-occurring with their group.
}
\details{
The source data often uses raw column names (e.g. \code{hex1}, \code{hex2}); the
caller is responsible for renaming those to \code{from} and \code{to} before passing
the edgelist here.
}
\examples{
el <- tibble::tibble(
  group = c("A", "A", "B"),
  from  = c("x", "y", "x"),
  to    = c("y", "z", "z")
)
prepare_contingency_table(el)

}
