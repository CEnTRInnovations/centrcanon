% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchoring.R
\name{calculate_anchoring_score}
\alias{calculate_anchoring_score}
\title{Calculate conceptual anchoring scores}
\usage{
calculate_anchoring_score(ca_result, cluster_assignments)
}
\arguments{
\item{ca_result}{A \code{CA} object from \code{\link[=run_correspondence_analysis]{run_correspondence_analysis()}}.}

\item{cluster_assignments}{An integer vector of PAM cluster IDs, one per
concept (i.e. \code{pam_result$clustering}).}
}
\value{
A tibble with one row per concept and columns:
\describe{
\item{concept}{character. Concept / descriptor name.}
\item{cluster}{integer. PAM cluster ID.}
\item{salience_raw}{double. CA column inertia.}
\item{dimensional_raw}{double. Sum of contributions to CA dims 1 & 2.}
\item{cluster_exemplar_raw}{double. Proximity-weighted cos2 score.}
\item{rank_salience}{integer. Dense rank of \code{salience_raw}.}
\item{rank_dimensional}{integer. Dense rank of \code{dimensional_raw}.}
\item{rank_exemplar}{integer. Dense rank of \code{cluster_exemplar_raw}.}
\item{combined_rank}{double. Mean of the three ranks.}
\item{anchoring_score}{double. Normalized to [0, 1].}
\item{percentile_rank}{double. 0–100.}
\item{anchoring_category}{ordered factor.
\verb{"Peripheral Element" < "Supporting Element" < "Secondary Anchor" < "Primary Anchor"}.}
\item{tier}{ordered factor. From \code{\link[=calculate_tier]{calculate_tier()}}.}
}
}
\description{
Combines three sub-scores — salience, dimensional definition, and cluster
exemplar proximity — into a normalized anchoring score for each concept
descriptor identified in a correspondence analysis.
}
\details{
Sub-scores are equally weighted. All three are dense-ranked, summed, and
min-max normalized to produce \code{anchoring_score}.
}
\examples{
\dontrun{
ct  <- prepare_contingency_table(edgelist)
ca  <- run_correspondence_analysis(ct)
pam <- run_pam_clustering(ca)
calculate_anchoring_score(ca, pam$clustering)
}

}
