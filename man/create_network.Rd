% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{create_network}
\alias{create_network}
\title{Create an undirected network graph from an edgelist}
\usage{
create_network(edgelist)
}
\arguments{
\item{edgelist}{A data frame or tibble with columns \code{from} and \code{to}.
An optional \code{weight} column is preserved as an edge attribute if present.}
}
\value{
An undirected \code{igraph} object with loops and multi-edges removed.
}
\description{
Builds an undirected \link[igraph:igraph]{igraph::igraph} object from a two-column edgelist,
removing loops and multi-edges.
}
\details{
The source data often uses capitalized column names (\code{From}, \code{To}); the
caller is responsible for renaming to lowercase \code{from} and \code{to} before
passing the edgelist here.
}
\examples{
el <- tibble::tibble(
  from = c("a", "b", "a"),
  to   = c("b", "c", "c")
)
create_network(el)

}
